---
description: Retoma el ciclo Gentek desde la última fase pausada
---

Ejecuta `npx @gentek/cli resume` con la herramienta Bash en el directorio actual.

Si no existe `.gentek/state.json`, sugiere correr `/gentek-init` primero.
