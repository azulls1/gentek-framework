---
description: Muestra estado actual del ciclo Gentek (fases completas, checkpoints, próximos pasos)
---

Ejecuta `npx @gentek/cli status` con la herramienta Bash en el directorio actual y muestra el output al usuario.

Si no existe `.gentek/`, sugiere correr `/gentek-init` primero.
